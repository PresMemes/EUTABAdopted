is_xenophobe_not_pac = {
	is_xenophobe = yes
	is_pacifist = no
}

is_xenophobe_and_pac = {
	is_xenophobe = yes
	is_pacifist = yes
}

eu_auth_imp = {
	is_autocracy = yes #has_authority = auth_imperial
	is_authoritarian = yes # Gestalts can't have ethics, so this should stop them from getting techs they shouldn't
}

eu_auth_other = {
	is_autocracy = no #NOT = { has_authority = auth_imperial }
	is_authoritarian = yes
}