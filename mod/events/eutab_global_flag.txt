namespace = eutab_flag
event = {
	id = eutab_flag.1
	hide_window = yes
	is_triggered_only = yes
	trigger = {
		NOT = {
			has_global_flag = eutab_active
		}
	}
	immediate = {
		set_global_flag = eutab_active
	}
}
